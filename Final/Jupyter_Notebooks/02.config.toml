# Remove bright detections
amplitude_tolerance = 1.0

# Remove large detections
sigmax_tolerance = 1.0
sigmay_tolerance = 1.0
sigmaz_tolerance = 1.0

# Remove low detection quality (high error) detections
quality_threshold_mu = 1.0
quality_threshold_sigma = 1.0